bintray {
    pkg {
        name = "$project.group:$project.name"
        issueTrackerUrl = "https://github.com/dmahapatro/grails-$project.name/issues"
        vcsUrl = "https://github.com/dmahapatro/grails-$project.name"
        version {
            attributes = ['grails-plugin': "$project.group:$project.name"]
            name = project.version
        }
    }
}